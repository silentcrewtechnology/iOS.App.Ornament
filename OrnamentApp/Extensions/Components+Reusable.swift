//
//  Components+Extensions.swift
//  OrnamentApp
//
//  Created by user on 03.07.2024.
//

import Components
import ArchitectureTableView
import UIKit

// Делаем update с пустым VP для сброса

extension ActivityIndicatorView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension AuthorizationButton: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension BadgeView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension ButtonView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension ButtonIcon: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension CardImageView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension CheckboxView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension ChipsView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension DividerView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension DotView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension HintView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension ImageView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension IndicatorView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputAmountTextField: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputAmountView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputMessageView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputOTPItemView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputOTPView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputPhoneNumberView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputSelectView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputTextField: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputTextView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension InputTextareaView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension LabelView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension PageControl: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension PaperView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension PaymentButton: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension RadioView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SectionMessageView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SegmentItemView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SegmentSliderView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SegmentView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SnackBarView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension SpacerView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension StepperItemView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension StepperView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TabItemView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TabsView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TagView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TapInsetView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TextBlockView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TileView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension TitleView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension ToggleView: Reusable {
    public func prepareForReuse() {
        update(with: .init())
    }
}

extension UIStackView: Reusable {
    public func prepareForReuse() { }
}

extension UIScrollView: Reusable {
    public func prepareForReuse() { }
}
